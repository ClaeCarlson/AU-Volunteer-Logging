<p id="notice"><%= notice %></p>

<h1>Hours</h1>

<div class="jumbotron" style="background-color: white">
<table class="table">
  <thead>
    <tr>
      <th>Hours id</th>
      <th>Volunteerid</th>
      <!--<th>Volunteer Name<th>!-->
      <th>Hoursworked</th>
      <th>Weekworked</th>

      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hours.each do |hour| %>
    <% hourname = Volunteer.find_by_sql("SELECT * FROM Volunteers WHERE volunteer_id = #{hour.id}");%>
      <tr>
        <td><%= hour.id %></td>
        <td><%= hour.volunteer_id %></td>
        <td><%= hour.hoursWorked %></td>
        <td><%= hour.weekWorked %></td>
        <td><%= link_to 'Show', hour %></td>
        <td><%= link_to 'Edit', edit_hour_path(hour) %></td>
        <td><%= link_to 'Destroy', hour, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<div>

<br>

<%= link_to 'New Hour', new_hour_path %>
